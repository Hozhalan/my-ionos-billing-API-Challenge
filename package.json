{
  "name": "ionos-billing-api-test-suite",
  "version": "1.0.0",
  "description": "Test suite for IONOS Billing API with endpoint coverage and schema validation",
  "main": "mock-server/server.js",
  "type": "commonjs",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:report": "jest --reporters=default --reporters=jest-html-reporter",
    "mock-server": "node mock-server/server.js"
  },
  "keywords": [
    "ionos",
    "billing",
    "api",
    "testing",
    "test-suite",
    "mock-server"
  ],
  "license": "ISC",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-basic-auth": "^1.2.1"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "jest": "^30.1.3",
    "jest-html-reporter": "^4.3.0",
    "supertest": "^7.0.0"
  }
}
